package com.andy.actor.business.mreaddr

import com.andy.actor.BizActorWorker
import com.andy.mode.TEvent

/**
 * @author andy
 */
class MReAddrSearchWorker extends BizActorWorker {
  def execute(event: TEvent[_]): Option[TEvent[_]] = {
    ???
  }
}